{
  "name": "svg-minify-brunch",
  "version": "0.0.1",
  "description": "Adds svg minify support to brunch.",
  "author": "Schoolshape (http://www.schoolshape.com/)",
  "homepage": "https://github.com/brunch/svg-minify-brunch",
  "repository": {
    "type": "git",
    "url": "git@github.com:brunch/svg-minify-brunch.git"
  },
  "main": "./lib/index",
  "scripts": {
    "prepublish": "rm -rf lib && coffee --bare --output lib/ src/",
    "test": "node_modules/.bin/mocha --require test/common.js"
  },
  "dependencies": {
  },
  "devDependencies": {
    "mocha": "1.11.0",
    "chai": "1.7.0"
  }
}
